import { PipeFiltreDatesPipe } from './pipe-filtre-dates.pipe';

describe('PipeFiltreDatesPipe', () => {
  it('create an instance', () => {
    const pipe = new PipeFiltreDatesPipe();
    expect(pipe).toBeTruthy();
  });
});
